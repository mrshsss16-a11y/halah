# 🐛 المشاكل — سجل واحد لكل فجوة معروفة

> **قائمة حية.** كل بند: المشكلة، الدليل، الأثر، الحل، الحالة. يُحدَّث فور اكتشاف
> مشكلة جديدة أو إغلاق واحدة. لا تُحذف البنود المغلقة — تُعلَّم ✅ مع تاريخ الإغلاق.
>
> آخر تحديث: 2026-09-06 · الدومين `halah.aura.sa` مؤكَّد حياً (200/200/401) (+ فحص أمني عميق: عزل · حقن prompt · تسريب أخطاء ·
> مصادقة · أسرار · CORS · rate limiting — ٢٨ استدعاء، ٥ من ٧ محاور بلا مشاكل جديدة)
>
> **مزامنة توثيق ↔ كود (2026-09-06):** أُعيد التحقق من كل بند بقراءة الكود مباشرة، لا نقلاً.
> **٦ بنود كانت وهمية أو باطلة** (P2 · P4 · P5 · P6 · P7 · P8) — خمسة منها كانت مُصلَحة في
> الكود قبل توثيقها، وواحد (P6) مستحيل تقنياً على Pages. **٢ بندان تأكّدا حقيقيين**
> (P18 · P21). البند الوحيد الذي كان `AGENT.md` §١٣ يدّعي إغلاقه خطأً هو **P18** (رقم
> التذكرة) — لا يزال مفتوحاً بدليل ملف:سطر. **التحقق المعلّق L4 حُسم:** جدول `error_log`
> **موجود فعلاً على D1 البعيد** — نسخة `backups/halah-tr-db_2026-09-06T04-52-55_full.sql`
> تحوي `CREATE TABLE error_log` (سطر ٥٣٠)، فهرسين (٥٦٨/٥٦٩)، وصف هجرة
> `0015_error_log.sql` مطبَّقاً `2026-09-06 03:06:33` بجدول `d1_migrations`.

---

## 🔴 حرج — يوقف الإنتاج أو يهدد بياناته

| # | المشكلة | الدليل | الأثر | الحل | الحالة |
|---|---|---|---|---|---|
| P1 | بوت واتساب متوقف | كان لا توكن دائم | ٣ رسائل عملاء بلا رد منذ 2026-07-23 (٤٥ يوماً) | توكن دائم مرفوع + منشور، متحقَّق (`/api/health` 200) | 🟡 التوكن حُلّ 2026-09-06 — **لكن اكتُشف أعمق أدناه (P27)** |
| P27 | **لا رقم واتساب أورا الحقيقي مربوط أصلاً** — الموجود رقم ميتا التجريبي الافتراضي فقط | صرّح المستخدم 2026-09-06: "مافي رقم لهالة إلى الآن، الموجود المؤقت" | حتى لو التوكن يعمل، البوت لا يصل لأي عميل حقيقي — رقم ميتا التجريبي لا يراه إلا مطوّرو التطبيق | **الحل الفوري (بلا حفظ رسائل):** "اسم عرض فقط" برقم نظيف — فوري. **الحل الدائم (يحفظ الرسائل):** P37 أدناه | 🟡 **قسمان:** حل فوري متاح الآن · حل دائم (Coexistence) يحتاج P37 |
| **P37** | **Tech Provider + Embedded Signup غير مبنيين** — رقم أورا الحالي لا يمكن ربطه بالبوت بلا فقدان رسائل ومحادثات (Coexistence يتطلب الاثنين، مؤكَّد من توثيق ميتا الحي 2026-09-06) | فحص مباشر لتوثيق ميتا: "You must already be a Solution Partner or Tech Provider" + "Onboarding Path: Embedded Signup Only — not through WhatsApp Manager" | صفر طريق بلوحة ميتا لربط رقم قائم بلا فقدان بيانات | بناء تسجيل Tech Provider (توثيق + مراجعة أعمال) + تدفق Embedded Signup بالكود (JS SDK + endpoint تبادل التوكن) | ⬜ **قرار مستخدم صريح 2026-09-06: "أهم ميزة، لازم نبنيها"** — يخدم هدفين: (أ) حفظ رسائل أورا الحالية بلا خسارة، (ب) ربط واتساب تجار سلة بضغطة واحدة لاحقاً — نفس البنية بالضبط. مرجع: `.claude/skills/whatsapp-embedded-signup/SKILL.md` |
| P2 | ~~`verifyWaSignature` يرجّع `true` عند غياب `WHATSAPP_APP_SECRET`~~ | **بند وهمي** — التحقق المباشر 2026-09-06: `functions/_lib/integrations/whatsapp.js:38` = `if (!appSecret) return false; // Fail closed` | لا أثر — الكود fail-closed أصلاً، البند كان نقلاً من نسخة قديمة بلا تحقق | لا شيء مطلوب | ✅ أُغلق 2026-09-06 — كان مُصلَحاً قبل توثيقه |
| P3 | صفر نسخة احتياطية لـD1 (كان الوضع حتى أمس) | لا سكربت، لا أمر بـ`package.json` | مطوّر واحد + هجرة خاطئة = فقدان بيانات دائم | `scripts/backup-db.mjs` + `npm run backup` | ✅ أُغلق 2026-09-05 |
| P4 | ~~٦ نقاط تستدعي AI بلا فحص حصة~~ | **وهمي شبه كلياً** — فحص بند-ببند 2026-09-06: `scan`/`sandbox`/`trendyol/qa` **غير موجودة بالإنتاج** (مؤرشفة بـ`docs/archive/deprioritized-features/functions/api/`) · `support.js` معفى بالتصميم (ودجت أورا نفسه، `storeId: "hala"`، محمي بـ`checkRateLimit` — `support.js:28`) · `admin/aura_whatsapp.js` معفى عمداً (مسار أدمن، `requireAdmin` بسطر ١١) · `whatsapp/webhook.js` **مُقاس فعلاً** (`checkAndConsumeMonthly` — `webhook.js:110-111`) | صفر تعرّض من الخمسة الأولى؛ الباقي مُقاس | لا شيء — البند يُغلق. الفرع الوحيد الباقي (fail-open بفحص الحصة) صار قراراً صريحاً، انظر ملاحظة أسفل الجدول | ✅ أُغلق 2026-09-06 — لا مسار منها كان فجوة حقيقية |
| P21 | `whatsapp/webhook.js` بلا `checkRateLimit` على الطلب نفسه — الحاجز الوحيد توقيع `X-Hub-Signature-256` | **لا يزال قائماً — تحقق 2026-09-06:** الملف يستورد `checkRateLimit` (سطر ١٥) لكن الاستدعاء الوحيد بسطر ١١٩ وهو **خنق تنبيهات داخلي** (`"wa_quota_check_failure"`)، لا بوابة على الطلب الوارد | لو تسرّب `WHATSAPP_APP_SECRET` أو ضعف التحقق (P2)، لا خط دفاع ثانٍ حتى ضد طلبات موقّعة بكثافة | إضافة `checkRateLimit` على الويبهوك مثل بقية المسارات الحساسة | ⬜ |
| P28 | فشل **فحص** الحصة بويبهوك واتساب يفشل مفتوحاً (fail-open) | `functions/api/whatsapp/webhook.js:107-121` — `checkAndConsumeMonthly(...).catch(...)` يرجّع `{ ok: true }` عند عطل D1/KV | رسائل غير محسوبة أثناء عطل قاعدة البيانات | **ليس دَيناً — قرار مدير 2026-09-06:** يبقى fail-open عمداً (البوت يرد أثناء عطل D1 بدل توقف كل التجار)، مع `logError` وتنبيه مُعدَّل (`checkRateLimit` بسطر ١١٩) | ✅ قرار مُتَّخذ 2026-09-06 — مغلق كقرار، لا يُعاد فتحه كدَين |
| P24 | ~~حقن prompt عبر `chat.js`~~ | `functions/api/chat.js:169` | كان يقبل `body.botInstructions`/`body.storeInstructions` من العميل ويدرجها بـsystem prompt | حُذف السقوط لقيمة العميل كلياً — صفر استخدام حقيقي بأي واجهة، حذف آمن ١٠٠٪ | ✅ أُغلق 2026-09-06 · ٣٨/٣٨ اختبار · منشور ومتحقَّق حياً |

---

## 🟠 عالي — يمس الأمان أو العزل بين العملاء

| # | المشكلة | الدليل | الأثر | الحل | الحالة |
|---|---|---|---|---|---|
| P5 | ~~لا تدقيق آلي لعزل المتاجر بالاستعلامات~~ | `scripts/audit-isolation.mjs` موجود، ومربوط بـ`package.json:11`: `"test": "node tests/api.test.mjs && node scripts/audit-isolation.mjs"` | — | السكربت يعمل ضمن `npm test` | ✅ أُغلق 2026-09-06 |
| P6 | ~~لا `observability` مفعّلة بـ`wrangler.toml`~~ | ❌ **باطل — مستحيل تقنياً، لا "لم يُنفَّذ":** Cloudflare Pages يرفض المفتاح صراحةً ("Configuration file for Pages projects does not support 'observability'") — مفتاح Workers-only. جُرِّب حياً و`npm run deploy` فشل فوراً (انظر `wrangler.toml:32-39`) | لا logs/traces مركزية عبر Cloudflare | **البديل مُنفَّذ:** تسجيل أخطاء مهيكل إلى D1 (`error_log`) + `/api/admin/errors`. **لا تُعِد إضافة المفتاح** ما لم يدعمه Pages | ❌ باطل 2026-09-06 — غير قابل للتنفيذ على Pages، مغطّى ببديل |
| P7 | ~~لا معرّف طلب (`requestId`) في السجلات~~ | `functions/_lib/core/respond.js:51` `generateRequestId()` — يُمرَّر لكل رد (`X-Request-Id` بسطور ٧٤/٧٧/٨٦/٩٨) ولكل `logError` (سطور ٨١، ٨٩) وبجسم الخطأ (٨٤، ٩٥) | — | مُنفَّذ | ✅ أُغلق 2026-09-06 |
| P8 | ~~لا صفحة أخطاء مركزية للأدمن~~ | `functions/api/admin/errors.js` موجود — `requireAdmin` (سطر ١٢) ثم `SELECT ... FROM error_log` (سطر ١٧)، حد افتراضي ٥٠ وأقصى ٢٠٠، وفلترة بـ`storeId` | — | مُنفَّذ | ✅ أُغلق 2026-09-06 |
| P9 | لا سطر تدقيق لقراءات الأدمن العابرة للعملاء | `requireAdmin` يتحقق من الهوية، لا يسجّل من قرأ بيانات أي عميل ومتى | لا مساءلة عند نزاع أو شك بإساءة استخدام | جدول `audit_log`: من، ماذا قرأ/عدّل، متى | ⬜ |
| P10 | رسائل صادرة (واتساب) بلا آلية قوالب/موافقة موثّقة | استرداد السلات وميزات مشابهة بالخطة | إرسال تسويقي غير مطلوب = خطر إيقاف رقم أورا بالكامل من Meta | قالب معتمد + سجل موافقة صريحة لكل عميل قبل أي إرسال صادر | ⬜ يحتاج قرار د أولاً |

---

## 🟡 متوسط — يمس الاستقرار أو التكلفة على المدى المتوسط

| # | المشكلة | الدليل | الأثر | الحل | الحالة |
|---|---|---|---|---|---|
| P11 | لا Queue أمام أي مسار كتابة كثيفة مستقبلي | بحث Cloudflare 2026: الكتابة المباشرة بمسار الطلب تصطدم بقفل D1 | محرك التقارير/معالجة البيانات (منصة أورا) لو بُني بمسار مباشر بدل طابور | Workers Queue أمام أي كتابة تحليلية مجمّعة | ⬜ يُقرَّر قبل بناء محرك التقارير |
| P12 | حد D1 الحقيقي تحت الحمل غير معروف محلياً | بحث خارجي: p99 يرتفع بحدة بعد ~٢٠٠ طلب/ثانية على كتابة صف واحد (رقم عام، غير مقيس على هذا المشروع) | قرار التخزين التحليلي (Q1 2027) قد يلزم أبكر مما خُطِّط | قياس فعلي على الحمل الحقيقي بعد الإطلاق المحدود، لا افتراض | ⬜ يُراقَب بعد الإطلاق |
| P13 | فجوة الريبليكا (Replication Gap) لم تُختبر صراحة | نمط معروف: كتابة ثم قراءة فورية من ريبليكا متأخرة = ٤٠٤ خادع | نفس فئة أعطال B4 الموثّقة (سر لا يُفعَّل إلا بعد نشر) — سبب مختلف، عرض مشابه | علم `primary` على القراءة الحرجة بعد كتابة مباشرة (مثال: بعد `app.store.authorize`) | ⬜ يُفحص بمسارات الكتابة-ثم-قراءة الفورية |
| P14 | `wrangler.toml` بدل `wrangler.jsonc` | الملف الحالي `.toml` | ميزات جديدة مستقبلية JSON-only فقط | هجرة صيغة، بلا تغيير منطق | ⬜ منخفضة الإلحاح |
| P15 | لا Durable Objects / Workflows بالمشروع، ومعرفتي بهما غير محمَّلة | لم تُحمَّل مهارة `durable-objects`، ولا مرجع Workflows | صندوق موحّد حي (منصة أورا) أو معالجة متعددة الخطوات دائمة قد تحتاجهما | تحميل المهارة/التوثيق عند الوصول لتلك الميزة، لا قبلها | ⬜ يُعاد التقييم عند م2.2/ج (منصة أورا) |
| P16 | حزمة `@cloudflare/workers-types` قد لا تكون أحدث نسخة بالمشروع | لم تُتحقَّق بهذه الجلسة | فجوة أنواع تراكمية بأخطاء صامتة وقت التطوير | `npm pack @cloudflare/workers-types` ومقارنة، أو `npm outdated` | ⬜ فحص روتيني |

---

## 🟢 منخفض — جودة، لا يهدد شيئاً فوراً

| # | المشكلة | الدليل | الأثر | الحل | الحالة |
|---|---|---|---|---|---|
| P17 | اختبارات تتحقق من التصدير فقط | `tests/api.test.mjs`، ٢٦ اختبار — موثّق بـ`AGENT.md` §١٣ | عزل المتاجر والتواقيع بلا تغطية منطقية حقيقية | اختبار واحد على الأقل يفشل لو كُسر العزل/التوقيع/الحصة لكل وحدة | ⬜ |
| P18 | رقم التذكرة: عمود DB + حساب من `id` بالكود | **لا يزال قائماً — تحقق مباشر 2026-09-06:** `functions/_lib/core/db.js:524-527` يكتب `ticket_code = AURA-{id padded 5}` بالعمود، و`functions/api/cron/reminders.js:112` **يعيد حسابه** من `booking.id` بدل قراءة العمود | مصدرا حقيقة؛ أي تغيير بصيغة الترقيم مستقبلاً يُنتج رقمين مختلفين لنفس الحجز | `reminders.js:112` يقرأ `booking.ticket_code` من الصف بدل إعادة البناء | ⬜ **لا يزال مفتوحاً — `AGENT.md` §١٣ كان يدّعي خطأً أنه موحَّد، صُحِّح 2026-09-06** |
| P19 | لا cron trigger أصلي بـ`wrangler.toml` رغم وجود `cron/reminders.js` | Pages لا يدعم cron triggers | حُلَّ بـWorker منفصل (`cron-worker/`) — الحل يعمل لكنه التفاف، لا حل أصلي | يبقى كما هو ما دام Pages هو الأساس المعماري المختار | ✅ محلول بالتفاف موثّق |
| P20 | استشارة PDPL قانونية فعلية غائبة | الكود يقدّم تقييماً تقنياً فقط، موثّق بصراحة | لا التزام قانوني مؤكَّد، فقط تقييم داخلي | استشارة قانونية فعلية قبل نطاق تجاري أوسع | ⬜ يتطلب طرف خارجي (أنت) |
| P22 | `health.js`، `cron/reminders.js`، و**`auth/salla/callback.js`** يرجعون `err.message`/`error.message` الخام عند فشل | فحص أمني 2026-09-06 — `health.js:84` · `reminders.js:143` · `salla/callback.js:93` | تسريب تفاصيل داخلية — `callback.js` الأخطر لأنه مسار عام قد يحوي تفاصيل استجابة Salla الداخلية | رسالة عربية عامة بدل الرسالة الخام بالثلاثة | ⬜ **رُفع لمتوسط بسبب `salla/callback.js`** |
| P23 | علم `HALA_DEBUG_AI=1` يفعّل حقل `detail` بالردود (`respond.js:64`) | فحص أمني 2026-09-06 | لا ثغرة حالياً — لكن لا تحقق يمنع ضبط العلم بالخطأ على الإنتاج | فحص صريح: العلم غير مضبوط ببيئة الإنتاج، أو حارس يرفض تفعيله هناك | ⬜ ملاحظة وقائية فقط |
| P25 | ~~قيمة سر افتراضية مكتوبة بالكود~~ | `functions/api/admin/aura_whatsapp.js:52` | كان `"hala-verify-secret"` احتياطياً بدل `WHATSAPP_VERIFY_TOKEN` — يخالف §٧ | الاحتياطي الآن `null` صراحة، لا نص يشبه سراً حقيقياً | ✅ أُغلق 2026-09-06 · ٣٨/٣٨ اختبار · منشور ومتحقَّق حياً |
| P26 | مقارنة `CRON_SECRET` بـ`!==` وليست timing-safe | `functions/api/cron/bulk_process.js:29` · `healthcheck.js:21` — فحص أمني 2026-09-06 | هجوم توقيت نظري لتخمين السر — خطر منخفض عملياً (سر طويل، مسار داخلي) لكن يخالف نمط timing-safe المتبع بتوقيعات الويبهوك | `crypto.subtle.timingSafeEqual` أو مكافئ | ⬜ منخفض |

---

## نقطة قرار معمارية — ليست مشكلة، بل ملاحظة

**Pages مقابل Workers (2026):** توصية Cloudflare الحالية للمشاريع الجديدة هي Workers
مباشرة (يوحّد الواجهة والخلفية بنشرة واحدة). هذا المشروع على Pages، وحارس
`verify-dist.mjs` مبني خصيصاً حول قيود Pages (منع Advanced Mode). **لا تُقترح
هجرة** — العائد غير واضح على الحجم الحالي، والحارس يعمل. تُعاد المراجعة فقط لو
احتجنا ميزة Workers-only أصيلة (Durable Objects حيّة، Workflows معقّدة) لا تلتف
عليها Pages Functions.

---

## 🟠 حواجب إنستغرام — مكتشفة 2026-09-06 ببحث توثيقي حي

المرجع الكامل: `.claude/skills/instagram-platform/SKILL.md`

| # | الحاجب | الدليل | الأثر | الحالة |
|---|---|---|---|---|
| P29 | **التطبيق `dev_mode` = صفر ويبهوك إنستغرام** | توثيق ميتا: "Your app must be published, regardless of app review status, to receive webhooks" · متحقَّق حياً: `app_status: dev_mode`, `is_live: false` | لا حدث إنستغرام يصل مهما كانت الصلاحيات — **حاجب مستقل عن App Review** | ⬜ نشر التطبيق خطوة إعدادات · ⚠️ يُفحص أثره على واتساب أولاً |
| P30 | **تناقض توثيقي: هل `comments` تحتاج Advanced Access؟** | صفحة App Review: "نشاط تملكه ← Standard ← مراجعة غير مطلوبة" · صفحة Webhooks: "must have successfully completed App Review (advanced access) to receive webhooks for `comments`" — **صفحتان حيّتان تتناقضان** | يحدد إن كان رد التعليقات ينطلق هذا الأسبوع أو بعد أسابيع مراجعة | ⬜ **يُحسم تجريبياً لا جدلاً** (خطوات بـ`YOUR_TASKS.md` بند ٥) |
| P31 | **الرسائل المباشرة بـStandard تصل فقط لمن له دور على التطبيق** | "Apps with Standard Access can only send messages to people that have a role on the app" | رد التعليقات ممكن · **الرسائل لعملاء حقيقيين تحتاج Advanced + App Review** | ⬜ مؤجَّل بمحفّز |
| P32 | **وسم `HUMAN_AGENT` يمنع الرسائل الآلية صراحةً** | "Required for Instagram Messaging API" + "Disallowed Usages: **Automated messages**" · وهو الوسم الوحيد المتاح لإنستغرام | **خارج نافذة ٢٤ ساعة لا رد آلي مشروع إطلاقاً** — البشري فقط. رد بوت تحته = مخالفة تعرّض حساب أورا للإيقاف | ⬜ قيد تصميم دائم، لا يُحل |
| P33 | **حد المعدل متغير: `4800 × Impressions` / ٢٤ ساعة** | صفحة Graph API Rate Limiting — قسم Instagram BUC | حساب خامل ← Impressions ≈ 0 ← **سقف ≈ صفر**. أخطر من حد سلة لأنه غير مرئي ويتغير يومياً | ⬜ يفرض الاعتماد على الويبهوك لا الاستطلاع |
| P34 | **توقيع إنستغرام يختلف عن واتساب** — يُحسب على **unicode-escaped** لا على البايتات المفكوكة | "we generate the signature using an escaped unicode version of the payload... `äöå` → `äöå`" | نسخ `verifyWaSignature` حرفياً **يفشل على أي نص عربي** — وكل محتوانا عربي | ⬜ قيد تنفيذ للمحوّل |
| P35 | ٣ حقول إعدادات ترفضها المراجعة | `terms_of_service_url` و`data_deletion_url` = `https://www.facebook.com/` · `contact_email_verified: false` منذ 2026-08-07 | أي مراجعة مستقبلية تُرفض شكلياً · وتفوتك تنبيهات ميتا الحرجة | ⬜ دقائق — `YOUR_TASKS.md` بند ٤ |

---

## P36 — محرك الوصف التجاري كان مفتوحاً للإنترنت بلا تسجيل دخول (حرج)

**الاكتشاف (تحقق حي 2026-09-07، بلا أي كوكي جلسة):**

```
POST https://halah.aura.sa/api/copy -d '{"name":"عطر عود ملكي","category":"عطور","price":"350"}'
→ {"ok":true,"result":"...وصف كامل...","seo":{...عنوان، slug، metaDescription، keywords، jsonLdSchema...}}
```

**السبب الجذري:** `resolveStoreId` (`functions/_lib/core/session.js:123`) يسقط لـ`"default-store"`
عند غياب الجلسة. هذا مقصود ومطلوب لودجت الموقع المجهولة، لكنه امتد بالوراثة لكل نقطة
تستدعي نموذج AI: `copy.js` · `chat.js` · `image.js` · `store/bulk/upload.js`.

**الأثر:** استنزاف مجاني للذكاء الاصطناعي على فاتورة المشروع · منافس يستخدم المحرك
التجاري الكامل (وصف + حزمة SEO + JSON-LD) بلا حساب · علامة حمراء بمراجعة أمان سلة.

**الإصلاح:** دالة جديدة `resolveMerchantStoreId` بـ`functions/_lib/core/session.js:156-183`
— نفس منطق `resolveStoreId` **ناقص** دلو `default-store` المجهول؛ ترمي
`401 LOGIN_REQUIRED` برسالة `"سجّل دخولك أولاً."`. طُبِّقت على:

| الملف:السطر | النقطة |
|---|---|
| `functions/api/copy.js:12,275` | `/api/copy` |
| `functions/api/chat.js:16,133` | `/api/chat` |
| `functions/api/image.js:13,26` | `/api/image` |
| `functions/api/store/bulk/upload.js:7,23` | `/api/store/bulk/upload` (يُجدول حتى ٥٠٠ توليد) |

**ما بقي مفتوحاً عمداً:** `/api/support` (ودجت زوار أورا نفسها، `storeId:"hala"` ثابت،
`checkRateLimit` ٢٠/دقيقة) و`/api/whatsapp/webhook` (محمي بتوقيع `X-Hub-Signature-256`).
كذلك تاجر سلة Easy-Mode بلا حساب لا يزال يُعرَّف بمعرّفه `m_` غير القابل للتخمين —
مسار مقصود وموثّق بـ`session.js` ومقاس بالحصة لكل مستأجر. تاجر لوحة سلة المضمّنة
غير متأثر: يحصل على كوكي جلسة حقيقي من `functions/api/auth/salla_embedded.js:67`.

**حارس الارتداد (M6):** `tests/api.test.mjs` — أربع حالات تؤكد `401 LOGIN_REQUIRED`
للنقاط الأربع بلا جلسة، وحالة خامسة تؤكد أن `resolveStoreId` **لا يزال** يرجّع
`default-store` لحركة الودجت المجهولة (حماية من إغلاق مفرط). ٦٦/٦٦ اختبار.

**الحالة:** ✅ أُغلق بالكود 2026-09-07 — بانتظار النشر والتحقق الحي.

**خطأ بوثيقتنا صُحِّح:** `PARALLEL_TRACKS.md` و`YOUR_TASKS.md` كانا يطلبان ربط إنستغرام بصفحة
فيسبوك وصلاحيات `instagram_manage_*` — أسماء **مسار Facebook Login**، تناقض قرارنا المتخذ
(Instagram Login). البيانات الحية تؤكد القرار: `pages_show_list`/`pages_read_engagement`/
`instagram_basic` كلها `REJECTED` على تطبيقنا.

---

## قاعدة الاستخدام

- **عند اكتشاف مشكلة جديدة:** تُضاف هنا فوراً بدرجة خطورة صريحة، قبل أي نقاش حل.
- **عند إغلاق مشكلة:** تُعلَّم ✅ مع تاريخ وسبب الإغلاق (commit أو ملف)، لا تُحذف.
- **لا حل بلا دليل.** كل بند يحمل مصدره (ملف، تحقق مباشر، أو بحث خارجي موسوم كذلك).
- **الأولوية أ (`PARALLEL_TRACKS.md`) تُقرأ مع هذا الملف** — كل معيار أمن/عزل هناك
  له بند مقابل هنا إن كان مخالَفاً اليوم.
