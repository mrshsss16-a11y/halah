# 📊 متابعة الحالة + خطتان متوازيتان

> تدقيق مباشر على الكود والـgit، لا نقل عن تقارير. 2026-09-06.

---

## ١. ما أُنجز فعلاً (مُتحقَّق بنفسي)

| البند | الدليل |
|---|---|
| نسخ احتياطي D1 | `scripts/backup-db.mjs` + `npm run backup` · ٤ نسخ موجودة |
| P24 حقن prompt بـ`chat.js` | السقوط لقيمة العميل محذوف · منشور |
| P25 سر افتراضي بالكود | `aura_whatsapp.js:52` صار `null` · منشور |
| P2 `verifyWaSignature` | `whatsapp.js:37` — `if (!appSecret) return false` ✅ (كان دَيناً وهمياً) |
| P5 تدقيق العزل الآلي | `scripts/audit-isolation.mjs` بـ`npm test` — ١٩ جدول، صفر استعلام غير معزول |
| D2 معرّف الطلب | `respond.js` + `errorLog.js` |
| D3 سجل الأخطاء | `admin/errors.js` + هجرة `0015` (⚠️ وجود الجدول على البعيد لم أتحقق منه — `7403`) |
| `image.js` عزل + حصة | `resolveStoreId` + `checkAndConsumeMonthly("image")` · ٢٠/شهر بـ`meter.js:24` |
| ردود جاهزة (ترحيب) | `intents.js` موصول بـ`webhook.js` — أول رسالة فقط، بلا استهلاك حصة |
| قرار fail-open للحصة | `flagQuotaCheckDegraded` + نافذة ٥/١٠ دقائق + تسجيل |
| **B5 تبويب "متجري"** | `dashboard.html` — `store/overview` + `usage` + آخر الطلبات |
| توكن Meta | مرفوع ومنشور · `/api/health` 200 |
| `halah.aura.sa` | حي ومؤكَّد (200/200/401) |

**الاختبارات: ٣٨/٣٨ + تدقيق العزل يمرّان.**

---

## ٢. 🔴 خطر فوري غير مسجَّل: كل شغل اليوم غير محفوظ بـgit

```
١٩ ملف معدَّل · ٣٠٩ إضافة / ٧٤ حذف · ١١ وثيقة جديدة
آخر commit: aed9010 — قبل كل شغل اليوم
```

**الأثر:** أمر واحد خاطئ (`git checkout .` · `git reset --hard` · انهيار قرص) = **ضياع يوم عمل كامل من أربعة وكلاء**. النسخة الاحتياطية تحمي قاعدة البيانات، **لا الكود**.

**ينطبق عليه Q4** (يُسقط عملاً حياً) → **لا يُؤجَّل**. أول بند بالخطتين أدناه.

---

## ٣. ما تبقّى فعلاً

### يحجب الإطلاق
| # | البند | المالك |
|---|---|---|
| P27 | ربط رقم أورا الحقيقي | **أنت** (ينتظر الجوال) |
| — | استكمال ملف سلة | **أنت** |
| — | تأكيد `error_log` على D1 البعيد | وكيل |

### قرارات معلّقة عليك
| # | القرار |
|---|---|
| ١ | زر الصور بالداشبورد — الآن أم بعد عيّنة جودة؟ |
| ٢ | تعبئة `CONTINUITY_PLAN.md` (٦ فراغات) |

### مؤجَّل بمحفّز
كل الباقي بـ`DEFERRED.md` — لا يُلمس خارج محفّزه.

---

# الخطتان المتوازيتان

**منطق التقسيم:** خطة تحتاجك (انتظار خارجي وقرارات)، وخطة لا تحتاجك إطلاقاً (كود بحت).
لا تعتمد أي منهما على الأخرى — تمشيان معاً بلا تعطيل متبادل.

---

## 🟦 الخطة الأولى — "الإطلاق" (تحتاجك)

**الهدف:** هالة تصل عميلاً حقيقياً.

| # | الخطوة | المالك | المخرج |
|---|---|---|---|
| L1 | **حفظ الشغل بـgit** (commit للكود ثم للوثائق) | وكيل | صفر عمل معرّض للضياع |
| L2 | ربط رقم أورا (Coexistence) + تحديث `WHATSAPP_PHONE_ID` + نشر | **أنت** ثم وكيل | رسالة اختبار يردّ عليها البوت |
| L3 | الرد على ٣ رسائل عملاء معلّقة | **أنت** | صفر رسالة بلا رد |
| L4 | تأكيد `error_log` على البعيد (نسخة جديدة + `grep`) | وكيل | D3 معلَنة منجزة بدليل |
| L5 | استكمال ملف سلة + متجر ديمو | **أنت** | الطلب مُرسل للمراجعة |
| L6 | تعبئة `CONTINUITY_PLAN.md` | **أنت** | صفر فراغات |
| L7 | تشغيل هالة على ٥ من عملاء أورا + مراقبة يومية | مشترك | معايير الخروج |

**معايير الخروج (لا توسيع قبلها):** ٢٠ محادثة بلا تدخل · ٥ حجوزات وصلت تذكرتها · صفر عطل صامت أسبوعاً.

---

## 🟩 الخطة الثانية — "المنتج" (لا تحتاجك)

**الهدف:** جاهزية ما بعد الإطلاق تُبنى بالتوازي، بلا انتظار أي طرف خارجي.

| # | الخطوة | يخدم |
|---|---|---|
| B1 | **عيّنة جودة الصور** — ١٠-١٥ منتج حقيقي متزامن، أنماط مختلفة، مراجعة بصرية | يحسم قرار زر الصور بدليل لا برأي |
| B2 | **D4 رسائل الفشل العربية** الموحّدة لكل تدفق | تجربة التاجر · صفر كود خام |
| B3 | **P17 الحد الأدنى**: ٣ اختبارات منطق (عزل · توقيع · حصة) | محفّزها "قبل أول تاجر" — يقترب |
| B4 | تصحيح تعليق `errorLog.js:12` الكاذب عن `observability` | توثيق يطابق الواقع |
| B5 | تحديث `PROBLEMS.md` (P2 · P4 · P5 · P7 · P8 منجزة) | السجل يطابق الكود |
| B6 | **محوّل إنستغرام** (بناء فقط، بلا تفعيل) | Standard Access — بلا انتظار Meta |
| B7 | **بوابة المراجعة البشرية** (`review_queue`) | شرط تفعيل B6 · أساس كل مخرج AI لاحق |

**قيد ملزم:** B6 يُبنى لكن **لا يُفعَّل على ردود حية** قبل B7. صفر رد آلي على منصة عامة بلا مراجعة إنسان.

---

## نقاط التقاء الخطتين

| متى | ماذا |
|---|---|
| بعد L2 | البوت حي → B3 (الاختبارات) تصير حرجة، لا وقائية |
| بعد L5 | اعتماد سلة يفتح تجاراً حقيقيين → محفّزات `DEFERRED.md` 🔵 تنطلق كلها |
| بعد B7 | بوابة المراجعة جاهزة → B6 يُفعَّل، ورد السوشيال يصير ممكناً |

---

## قاعدة التشغيل

1. **L1 أولاً، قبل أي شي.** عمل غير محفوظ = عمل غير موجود.
2. الخطتان تمشيان معاً — **لا تنتظر إحداهما الأخرى** إلا بنقاط الالتقاء أعلاه.
3. كل بند ينتهي بـ`npm test` (٣٨/٣٨ + تدقيق العزل) ثم نشر ثم تحقق حي.
4. أي مشكلة جديدة تُسجَّل بـ`PROBLEMS.md`، وتُجدوَل بـ`DEFERRED.md` إن لم تستوفِ Q1–Q5.
