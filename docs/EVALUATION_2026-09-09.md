# 🧪 تقييم المشروع الكامل — 2026-09-09

> فريق ستة وكلاء متخصصين عمل بالتوازي (٣ أوبس: الأمن، المنتج، الذكاء الاصطناعي · ٣ سونيت: جودة
> الكود، الوثائق، التشغيل). كل واحد قرأ الكود فعلياً، لا الوثائق. خط الأساس: ٤٥٢ اختباراً تمر،
> ٥٩ نقطة خدمة، ٢٦ هجرة، شجرة نظيفة عند `9563b38`.
>
> **هذا الملف = المرجع التنفيذي الحالي.** يكمّل `COMPLETION_PATH.md` (المراحل ٠-٦) بما كشفه
> التقييم بعد المرحلة ٤.

---

## ١. نسبة الاكتمال

| المجال | النسبة | المقيّم | أثقل خصم |
|---|---|---|---|
| الأمن | ٨٣٪ | أوبس | `support.js` يقبل `storeId` من الجسم بلا تحقق — يلتف على العزل كله (N1) |
| المنتج وتجربة التاجر | ٦٨٪ | أوبس | مدخل الرحلة ينتهي بـJSON خام بعد ربط سلة؛ استعادة كلمة المرور مكسورة؛ صفحة الهبوط تعد بمنتج آخر |
| الذكاء الاصطناعي | ٦٠٪ | أوبس | حارس الأسعار يمرّر «٥٠٠ ريال» بالأرقام الهندية ويغطي قناة واحدة؛ صفر دفاع ضد حقن البرومبت بأربعة مسارات |
| التشغيل | ٦٥٪ | سونيت | `/api/health` لا يفشل HTTP أبداً؛ لا نبض للـcron؛ هجرة كانت معلّقة على الإنتاج (طُبّقت اليوم) |
| جودة الكود | ٦٢٪ | سونيت | `timingSafeEqual` معرّفة ٤ مرات؛ ٩٣ خطأ مبتلعاً منها ٨ تمس المصادقة؛ SQL خام بـ٩ ملفات مصادقة |
| الوثائق | ~٨٠٪ | سونيت | أرقام عدّادات غير متطابقة؛ حالة زر الصور متأخرة عن الكود |
| **الإجمالي المرجَّح** | **٦٦٪** | | الأوزان: أمن ٢٠ · سلة ٢٠ · واتساب ١٥ · وصف/سيو ١٥ · ودجت ١٠ · تشغيل ٨ · فوترة ٧ · إنستغرام ٥ |

**ما يرفعها فوق ٦٦٪ لا يحتاج مرحلة تطوير:** الفجوات المكتشفة محدودة النطاق (ملف وسطر لكل واحدة). ما يبقيها تحت ١٠٠٪ خارج الكود: اعتماد سلة، رقم أورا، مزوّد الدفع، مزوّد البريد، ميتا.

---

## ٢. الفجوات المكتشفة (بالأولوية)

### 🔴 يمنع الإطلاق — يُصلَح الآن
| # | الفجوة | الدليل | المالك |
|---|---|---|---|
| N1 | `support.js:69` — `merchantId = body.storeId || "hala"` بلا تحقق: قراءة ذاكرة عملاء متجر آخر، تسريب تعليماته ورقمه، استنزاف حصته | أمن | كود |
| U1 | `auth/salla/callback.js:234` يرجّع JSON خاماً بلا جلسة ولا تحويل | منتج | كود |
| U2 | استعادة كلمة المرور: `login.html:334` يعرض `data.otpCode` والخادم لا يرجّعه؛ التسليم واتساب فقط لمن له صف | منتج | كود (تعطيل صادق) + مالك (Resend) |
| U3 | `index.html` يدّعي: سلات متروكة (مؤرشفة)، «٧٠٪ من زوارك» بلا مصدر، «AES-256»، «ربط رسمي مع زد» (مؤرشف) | منتج | كود |
| U4 | النشر المفرد يدهس وصف التاجر بلا تراجع (`store/publish.js` لا يكتب `original_description`) | منتج | كود |
| A1 | حارس الأسعار: `\d` لا يطابق `٠-٩`؛ يغطي `support.js` فقط، لا واتساب ولا إنستغرام | ذكاء | كود |
| A2 | حقن برومبت: `chat_summary` من زائر مجهول يُحقن بنص نظام واتساب (`webhook.js:177`)؛ وصف سلة، `custom_instructions`، RAG — كلها بلا محدِّدات | ذكاء | كود |
| A3 | `chat.js:268` يرجّع `score: 9, refined:false, guarded:false` مكتوبة بالكود — ادعاء نظام غير موجود (§11) | ذكاء | كود |
| A4 | `copy.js:313` فشل تحليل JSON ⇒ ينشر مخرج النموذج الخام كوصف | ذكاء | كود |

### 🟠 عالٍ — نفس الدفعة
| # | الفجوة | الدليل | المالك |
|---|---|---|---|
| N2 | `gateway.js:345` `askVisionAI` يجلب أي URL بلا قائمة سماح ولا حد حجم (SSRF) — نفس نمط P45 غير معمَّم؛ `imageProvider.js` مثله | أمن | كود |
| N3 | `rateLimit.js:12,50` fail-open على المصادقة (login/signup/forgot/reset) | أمن | كود |
| N4/N5 | `store/context.js` و`store/logo.js` **يكتبان** بـ`resolveStoreId` — حقن تعليمات دائمة بمعرّف `m_` | أمن | كود |
| N6 | `store/publish.js` بلا `checkRateLimit` (P48 نصف مغلق) | أمن | كود |
| N7 | Turnstile اختياري بيد العميل (`if (body.turnstileToken)`) | أمن | كود |
| O1 | `health.js:75` يرجّع 200 حتى عند فشل داخلي | تشغيل | كود |
| O2 | لا نبض للـcron: توقف `cron-worker` لا يكتشفه أحد | تشغيل | كود |
| O3 | لا فحص لصلاحية توكن واتساب/سلة قبل الفشل | تشغيل | كود |
| Q1 | `timingSafeEqual` بـ٤ نسخ (`session.js:41`, `oauthState.js:39`, `reset_password.js:23`) بدل `crypto.js:timingSafeEqualStr` | كود | كود |
| Q2 | أخطاء مبتلعة تمس الأمن: `login.js:52,56`، `session.js:65,79,97` (إبطال الجلسة قد يفشل بصمت)، `webhooks/salla.js:217` (توقيع مرفوض بلا أثر) | كود | كود |
| A5 | مسار الرؤية الاحتياطي `llama-3.2-11b` إنجليزي فقط ثم يُلزَم الوصف بذكر ما رآه | ذكاء | كود |
| A6 | `persona/` المرجعي يحيل لمسار خاطئ ويُسقط قاعدة السعر؛ لا مرجع لشخصية واتساب | ذكاء | كود |
| U5 | تبويب «الوكيل» يعد بما لا يُسلَّم: لا كود ودجت للتاجر، واتساب معطّل بلا `WA_SIGNUP_CONFIG_ID` | منتج | كود (صياغة صادقة + كود تركيب) |
| U6 | `support.js:134` رقم `966545149591` افتراضي بالكود (نفس P49) | منتج | كود |

### 🟡 متوسط/منخفض — تُغلق بنفس الدفعة إن رخصت
N8 `console.warn` ببريد حقيقي · N9 لا HSTS · N10 `cart_recovery_sim` بيانات مفبركة · Q3 `hashOtp`/`EMAIL_RE` مكررة · U7 التراجع يترك حقول السيو (رسالة صادقة) · U8 بطاقة المنتج لا تتحدث بعد النشر · U9 شريط الجملة بلا تنبيه «تبدأ خلال ~١٠ دقائق» · U10 `alert()` · U11 ألوان الأخطاء موحّدة · U12 الجوال (٣ كسور) · A7 `parseSeoResponse` regex جشع + حد أدنى للميتا + قصّ العنوان وسط الكلمة · A8 `seedKeywords` تحوّل الاسم لكلمات حشو · A9 `selectTtl` يكاش الدعم ٢٤ ساعة · O4 `AGENT.md §١٠` ناقص ٨ أسرار (`CRON_SECRET`…) · O5 النسخ الاحتياطي يدوي محلي بلا اختبار استعادة · D1 أرقام الوثائق.

### ⚪ يعتمد على المالك أو طرف خارجي (لا يقدر الكود عليه)
رقم أورا الحقيقي · ملف سلة + الاعتماد · Resend (يفعّل U2 بالكامل و3.3) · مزوّد الدفع · Business Verification وإنستغرام (ميتا) · Tech Provider · PDPL · نسخ احتياطي خارج الجهاز (قرار تخزين R2) · ٥ عملاء الإطلاق.

---

## ٣. خطة الإصلاح — أربع دفعات متوازية

| الدفعة | الوكيل | الملفات المملوكة حصراً | البنود |
|---|---|---|---|
| **أ — الأمن** | أوبس | `_lib/core/*`، `api/auth/*`، `api/support.js` (سطور الهوية والرقم)، `store/{context,logo,publish}.js`، `_headers`، `admin/aura_whatsapp.js`، `gateway.js:askVisionAI`، `imageProvider.js`، `webhooks/salla.js` | N1–N10، Q1، Q2، Q3، U6 |
| **ب — الصدق والذكاء** | أوبس | `_lib/ai/*`، `api/chat.js`، `api/copy.js`، `whatsapp/webhook.js`، `instagram/webhook.js`، `persona/` | A1–A9 |
| **ج — تجربة التاجر** | سونيت | `dashboard.html`، `login.html`، `index.html`، `auth/salla/callback.js`، `store/review/decide.js`، `widget.js` | U1–U5، U7–U12 |
| **د — التشغيل والوثائق** | سونيت | `api/health.js`، `api/cron/*`، `cron-worker/`، `AGENT.md`، `docs/*` | O1–O5، D1 |

**قواعد الدفعة:** كل وكيل يكتب اختباراته بملف مستقل `tests/<الدفعة>.test.mjs` (لا يلمس `api.test.mjs`)، يشغّل `npm test` قبل التسليم، لا ينشر، لا يكتب على ملف خارج ملكيته. الدمج والنشر والتحقق الحي من المنسّق.

**بوابة الإتمام:** كل البنود 🔴 و🟠 ✅ بالكود + اختبار يحرس كل واحد + `npm test` أخضر + نشر + smoke + تحقق حي لـN1/U1/A1/O1. عندها الاكتمال التقني يتجاوز ٨٥٪، والباقي بيد المالك والأطراف الخارجية.
