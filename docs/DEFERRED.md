# ⏭️ المؤجَّل — دفتر الدَين المُدار

> **الغرض:** يمنع تشتت التنفيذ. كل خطأ أو نقص لا يستحق التوقف الآن يُسجَّل هنا
> بمحفّز عودة صريح، بدل أن يُصلَح فوراً فيكسر تسلسل الخطة، أو يُنسى فيتحوّل لدَين صامت.
>
> **مؤجَّل ≠ منسي.** كل بند هنا يحمل **محفّزاً** — حدث محدد يُعيده للتنفيذ إجبارياً.
>
> كُتب 2026-09-06 · يُقرأ مع `PROBLEMS.md` (السجل الكامل) و`PARALLEL_TRACKS.md` (المعايير).

---

## بوابة القرار — يُصلَح الآن أم يُؤجَّل؟

**لا يُؤجَّل** أي خطأ ينطبق عليه واحد من هذي الخمسة:

| # | المعيار | لماذا |
|---|---|---|
| **Q1** | يُنتج مخرجاً **خاطئاً أو مفبركاً** يصل عميلاً | يكسر قاعدة الصدق (`AGENT.md` §١١) — أثمن ما بُني |
| **Q2** | يكسر **عزل المتاجر** أو يسرّب بيانات بينها | خسارة ثقة غير قابلة للاسترجاع |
| **Q3** | يكشف **سراً أو بيانات شخصية** (PII) | مخاطرة قانونية وأمنية |
| **Q4** | يقدر يُسقط **خدمة حية** أو يوقف الرد على عملاء | أثر تشغيلي مباشر |
| **Q5** | **يحجب الإطلاق** فعلياً | يعطّل كل ما بعده |

**كل ما عدا ذلك يُؤجَّل** — ويُسجَّل هنا بمحفّزه.

**قاعدة المدير:** لا يُصلَح بند مؤجَّل خارج محفّزه إلا بقرار صريح. "شفته وأنا مارّ فأصلحته"
هو بالضبط النمط اللي يفكك الخطة ويحوّل يوم عمل إلى ثلاثة.

---

## المؤجَّل الآن

### 🔵 محفّزه: قبل ربط أول تاجر حقيقي (غير أورا)

| # | البند | لماذا يُؤجَّل | لماذا يعود عندها |
|---|---|---|---|
| P21 | `whatsapp/webhook.js` بلا `checkRateLimit` | الحاجز الفعلي (توقيع HMAC) **مؤكَّد fail-closed** — هذي طبقة دفاع ثانية لا وحيدة | تعدد التجار يرفع سطح الهجوم وقيمة الهدف |
| P22 | `salla/callback.js` يرجّع `error.message` خام | مسار OAuth يمرّ به **أورا فقط** اليوم | يصير مساراً عاماً يمر به كل تاجر |
| P9 | لا سجل تدقيق لقراءات الأدمن العابرة للمتاجر | الأدمن الوحيد اليوم = مالك المشروع نفسه؛ لا طرف ثانٍ ليُساءل | دخول أي شخص ثانٍ لصلاحية الأدمن |
| ~~P5~~ | ~~تدقيق العزل الآلي~~ | — | ✅ **أُنجز 2026-09-06** — `scripts/audit-isolation.mjs` مربوط بـ`npm test`، تحقُّق مباشر: ١٩ جدول مستأجر، صفر استعلام غير معزول |
| P17 | اختبارات منطق حقيقية (عزل · توقيع · حصة) | ٣٨/٣٨ تمرّ، والتحقق الحي يغطي المسارات الحرجة مؤقتاً | **الحد الأدنى منها (٣ اختبارات) يعود قبل التاجر الأول، لا بعده** |

### 🟢 محفّزه: قبل بناء ميزة بعينها

| # | البند | المحفّز |
|---|---|---|
| P11 | لا Queue أمام الكتابة الكثيفة | **قبل أول سطر بمحرك التقارير** (المسار ج، أسبوع ٥) — لا يُبنى بمسار طلب مباشر |
| P13 | فجوة الريبليكا (كتابة ثم قراءة فورية) | **قبل تفعيل ربط سلة الذاتي للتجار** — مسار `app.store.authorize` بالضبط |
| P15 | Durable Objects / Workflows غير محمَّلة | **قبل** الصندوق الموحّد أو أي معالجة متعددة الخطوات دائمة |
| P10 | آلية قوالب الرسائل الصادرة + سجل الموافقة | **قبل أول ميزة صادرة** (سلة متروكة · عروض · تنبيه مخزون) — S13 |

### ⚪ محفّزه: بعد الإطلاق المحدود (بيانات حقيقية)

| # | البند | المحفّز |
|---|---|---|
| P12 | حد D1 الحقيقي تحت الحمل | ٢٠ متجراً نشطاً — يُقاس فعلياً بدل الاعتماد على رقم عام خارجي |
| P3 (تكلفة) | معايرة سقوف التكلفة | نفس اللحظة — الأرقام الحالية مخمّنة بالكامل |

### ⚫ محفّزه: تنظيف مجدول (لا استعجال)

| # | البند | ملاحظة |
|---|---|---|
| P14 | `wrangler.toml` → `.jsonc` | شكلي · يعود عند أول ميزة JSON-only |
| P16 | تحديث `@cloudflare/workers-types` | روتيني · مع أي `npm outdated` دوري |
| P23 | حارس يمنع `HALA_DEBUG_AI=1` على الإنتاج | وقائي · لا ثغرة حالية |
| P26 | `CRON_SECRET` مقارنة timing-safe | نظري · السر طويل والمسار داخلي |
| **جديد** | تعليق `errorLog.js:12` يدّعي أن `observability` مفعَّل | **توثيق كاذب عن حالة النظام** — يُصحَّح بأول كنسة توثيق |

### 🟣 خارج سيطرتنا (طرف ثالث)

| # | البند | المحفّز |
|---|---|---|
| P20 | استشارة PDPL قانونية | قبل التوسّع التجاري خارج الإطلاق التجريبي |

---

## ما **لا** يُؤجَّل (مفتوح الآن)

| # | البند | المعيار المنطبق |
|---|---|---|
| **P27** | لا رقم واتساب أورا حقيقي مربوط | **Q5** — يحجب الإطلاق كلياً |
| **سلة** | استكمال ملف الشركاء | **Q5** — أطول مسار انتظار خارجي بالمشروع |
| **تحقق D3** | تأكيد وجود `error_log` على D1 البعيد بنسخة احتياطية جديدة | لا إعلان إنجاز بلا تحقق حي (مبدأ ٣) |

---

## قواعد الدفتر

1. **كل بند مؤجَّل يحمل محفّزاً.** بلا محفّز = دَين صامت، لا تأجيل مُدار.
2. **المحفّز يُفحص عند كل معلم بالخطة** — قبل بدء أي أسبوع جديد، اقرأ هذا الملف واسأل: هل انطبق محفّز؟
3. **بند يستوفي Q1–Q5 لاحقاً يخرج فوراً** من التأجيل، ولو كان مؤجَّلاً بالأمس. الظروف تتغير: مسار داخلي يصير عاماً، وأدمن واحد يصير اثنين.
4. **لا إضافة بند هنا بلا تسجيله بـ`PROBLEMS.md` أولاً** — هذا دفتر جدولة، لا سجل مشاكل موازٍ.
5. **المراجعة إلزامية عند:** كل إطلاق · دخول أي شخص جديد للفريق · أي تغيير بنطاق المستخدمين (أورا → تجار).
