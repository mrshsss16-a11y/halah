# دليل: المهام التقنية المتبقية عليّ (تحديث 2026-07-19)

مو مواصفة تصميم — دليل تتبّع بسيط، يُحدَّث كل ما نخلص بند. كل بند مقسوم: **وش أسويه أنا بالضبط**، و**وش محتاجه منك قبل ما أقدر أبدأ**.

---

## ١. دومين `halah.aura.sa` — حرج

**الحالة:** 522 (SSL pending) — كان معلّق من جولة نقل سابقة بين مشاريع Cloudflare.

**وش أسويه:** أفحص حالة الشهادة عبر Cloudflare API، أتأكد إعدادات DNS صحيحة (CNAME/A record يشير لمشروع `hala-ai-os` مو المشروع القديم `halah-dashboard`)، أعيد تفعيل التزويد لو لقيت خلل.

**محتاج منك:** ولا شي، إلا لو المشكلة بمستوى تسجيل النطاق نفسه (مسجّل النطاق، مو Cloudflare) — بعطيك تفاصيل لو صار كذا.

---

## ٢. الجانب التقني لمراجعة سلة — جزء مني جاهز، جزء ينتظرك

| البند | مين مسؤوله | الحالة |
|---|---|---|
| Webhook URL + تحقق توقيع HMAC | أنا | ✅ جاهز، مبني ومختبر (`functions/api/webhooks/salla.js`) |
| صفحة سياسة الخصوصية | أنا | ✅ منشورة (`/privacy.html`) |
| Scopes المطلوبة موثّقة | أنا | ✅ موثّقة بـ`README.md` |
| حساب [salla.partners](https://salla.partners) + إنشاء التطبيق | **أنت** | ⬜ لسا ما بدأ |
| الأسرار الثلاثة (`SALLA_APP_ID`, `SALLA_CLIENT_ID/SECRET`, `SALLA_WEBHOOK_SECRET`) | **أنت تعطيني القيم، أنا أحطها** | ⬜ ينتظر حساب الشركاء أولاً |
| وصف التطبيق + أيقونته ببوابة الشركاء | **أنت** (محتوى)، أنا أراجع الصياغة لو تبي | ⬜ ينتظر حساب الشركاء |
| اختبار تثبيت حقيقي على متجر ديمو | **الاثنين معاً** (تحتاج متجر سلة تجريبي فعلي) | ⬜ آخر خطوة |

**وش محتاجه منك أول خطوة:** افتح حساب [salla.partners](https://salla.partners)، أنشئ تطبيق بوضع Easy Mode. أول ما يطلع لك `App ID`، `Client ID`، `Client Secret` — أعطنيهم وأنا أحطهم فوراً.

---

## ٣. فحص شامل نهائي لكل الميزات الحية

**وش أسويه:** فحص حي متسلسل لكل مسار (الفحص المجاني، توليد الأوصاف، استوديو الصور، دردشة الموقع، Trendyol، لوحة الأدمن) بحساب حقيقي، أوثّق أي خلل ألقاه وأصلحه فوراً — نفس أسلوب الفحص المستخدم طول المشروع.

**محتاج منك:** ولا شي، أبدأه لحالي أول ما تعطيني إشارة.

---

## قرارات تنتظرك بس (مو مهام تقنية، خارج نطاقي)

- مصير D1 القديمة `hala_db` (٣٢ جدول فاضي) — حذف أو إبقاء؟
- حذف الجداول legacy بـ`halah-tr-db` (استبدلتها الجداول الجديدة)
- مصير مشروع Pages القديم `halah-dashboard`
- استعادة حساب واتساب أورا من ميتا (توثيق هوية، رقم بديل) — خارج نطاقي تماماً
