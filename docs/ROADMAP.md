# 🗺️ خارطة طريق الإطلاق — هالة / أورا للتسويق

> **الوثيقة المرجعية الوحيدة للإطلاق.** تحلّ محل الخطط الثلاث السابقة وتنظّمها:
> `LAUNCH_PLAN.md` (تكتيكي) · `PRODUCT_PLAN.md` (نقد استراتيجي) · `UPGRADE_PLAN.md` (دَين تقني).
> تلك تبقى للتفاصيل والحيثيات؛ **الترتيب والأولويات من هنا.**
>
> القرار المعتمد (2026-08-07، مُصحَّح): **الهدف الأساسي تجار سلة — مو مؤجَّل.**
> واتساب والموقع **قنوات التواصل** بس (مو "بديل" عن سلة) — بوابة الميزات لتاجر سلة
> تمر عبرهما. ترتيب الميزات لتاجر سلة: **وصف المنتجات (١) ← الصور (٢) ← رفع
> بالجملة (٣) ← منصات ثانية كتيك توك (توسّع مستقبلي، مو الآن).**
> آخر تحديث: 2026-08-07.

---

## القيد الحاكم (اقرأه قبل أي شيء)

**من هو العميل؟ تاجر متجر سلة.** واتساب وودجت الموقع هما القنوات اللي التاجر (وعملاؤه)
يتعاملون منها مع المنتج — مو مسار منفصل عن سلة. بوت خدمة عملاء أورا (اللي طوّرناه
ونختبره الآن) هو **نفس محرك المحادثة** اللي يخدم عملاء أي تاجر سلة لاحقاً — تجربتنا
عليه الآن = تحقق حي قبل ما نعمّمه على تجار حقيقيين.

**ترتيب أولوية الميزات لتاجر سلة (صارم — لا تقفز خطوة):**

| # | الميزة | الحالة | ملاحظة |
|---|---|---|---|
| 1 | وصف المنتجات (SEO + سوق سعودي) | ✅ مبني وموصول (`copy.js` + تبويب الاستوديو) | الركيزة الأولى، مكتملة |
| 2 | صور المنتجات | 🟡 مبني، بيتا أدمن فقط (`33abd24`) | يحتاج مسار للتاجر لاحقاً — ليس الآن |
| 3 | رفع منتجات بالجملة | ⬜ **لا يوجد كود حالياً** — تحقق: صفر ملفات bulk/batch بالمشروع | بند جديد، يُضاف للخطة |
| 4 | منصات ثانية (تيك توك، إلخ) | ⬜ توسّع مستقبلي بعيد | لا عمل عليه — راجع `COMPETITOR_GABSTER.md` (Gabster لا ينافسنا بسلة الآن، لكن يهدد مستقبلاً ببوتات التواصل — سبب إضافي نعمّق سلة أولاً بدل التمدد المبكر) |

**القنوات (النطاق الحالي — لا توسيع قبل ثبات هذا):**

| | بوت واتساب | ودجت شات الموقع |
|---|---|---|
| الباك-إند | `whatsapp/webhook.js` — جاهز، مُختبر | `api/support.js` — جاهز، **غير موصول بأي واجهة** |
| المعطّل | توكن Meta دائم | لا شي — واجهة فقط |
| مَن يملكه | أنت (دقائق) | أنا |
| الجاهزية التقنية | ~٩٥٪ | ~٨٠٪ (باك-إند كامل، ناقص الودجت المرئي) |

**النتيجة:** نطلق القناتين خلال أيام (كل المعطّلات بأيدينا)، وباستمرار نبني عمق
سلة (٢→٣) خلفهما — **الاثنان يتقدمان معاً، مو تسلسل "الأول ثم الثاني منفصلين".**

---

## المرحلة 0 — مكتملة ✅

| البند | الحالة |
|---|---|
| إغلاق ٣ ثغرات مصادقة حرجة | ✅ `47f569b` |
| حماية OAuth من CSRF (سلة/زد) | ✅ `42f67ef` |
| حسم المعمارية + حارس نشر آلي | ✅ `566d52d` |
| توثيق ملزم لأي وكيل | ✅ `03717af` |
| إصلاح `/api/stats` | ✅ `0064f33` |
| **نموذج التسجيل المفقود** | ✅ `e4a3064` |

الأساس الأمني والتقني **جاهز للإنتاج**. ما تبقّى تشغيلي ومنتَجي.

---

## 🟦 المسار 1 — واتساب + الموقع (إطلاق خلال ٣ أيام)

### م1.1 — رفع الحجب (يوم 1) — **أنت**
| # | المهمة | التفصيل |
|---|---|---|
| A1 | **توكن Meta دائم** | Business Settings → System Users → صلاحيتا `whatsapp_business_messaging` + `whatsapp_business_management` → Generate Token بلا انتهاء |
| A2 | **ربط رقم أورا الحقيقي** | WhatsApp Manager → Add phone number → **Coexistence** (يبقي تطبيق واتساب بزنس شغالاً) |

> ⚠️ البوت متوقف منذ 2026‑07‑23. ثلاث رسائل عملاء بلا رد. A1 يفكّه.

### م1.2 — إكمال المنتج (يوم 1‑2) — **أنا**
| # | المهمة | لماذا |
|---|---|---|
| A3 | ربط `consultation.html` | ✅ **تم** (`0f5dadc`) — كانت أسوأ من "ميتة": تولّد رقم تذكرة عشوائي وتدّعي حجز ناجح بلا حفظ فعلي + بيانات عميل وهمية بالكود. اتربطت بـ`/api/consultation/book.js` (جديد) الذي يحجز فعلياً عبر D1، وأُزيل جدول الحجوزات العام (كان سيسرّب جوالات كل الزوار — الجدول بلا `merchant_id` وبلا مصادقة) |
| A4 | توحيد رقم التذكرة | ✅ **تم مع A3** (`0f5dadc`) — `bookingTicket()` المكررة بـ`webhook.js` أُزيلت، `saveConsultationBooking` بـ`db.js` صار المصدر الوحيد |
| A5 | تفعيل cron التذكيرات | الكود موجود، الـ trigger مفقود من `wrangler.toml` |
| A6 | مراقبة + تنبيه | عطل صامت استمر ١١ يوماً؛ الهدف: كشف < ١٥ دقيقة |
| A7 | **ودجت شات الموقع** | ✅ **تم** (`a013e04`) — زر عائم + لوحة دردشة بـ`index.html`، مربوط بـ`/api/support.js`، تحويل واتساب فعّال، تحقق حي بالمتصفح |

### م1.3 — قرارات محتوى (يوم 2) — **أنت**
- ماذا يُعرض مكان العدّادات الصفرية؟ (اقتراح: صياغة نوعية بدل أرقام فارغة)
- مراجعة صدق كل رقم/ادعاء بصفحة الهبوط

### م1.4 — 🚀 إطلاق محدود (يوم 3)
**٥‑١٠ عملاء أوائل، مراقبة لصيقة.**

**معايير الخروج (لا إطلاق موسّع قبلها):**
- ٢٠ محادثة حقيقية بلا تدخل يدوي
- ٥ حجوزات استشارة وصلت تذكرتها وتذكيرها
- صفر عطل صامت لأسبوع

---

## 🟩 المسار 2 — عمق منتج سلة (نشط، مستمر بالتوازي مع المسار 1)

> **مُصحَّح 2026-08-07:** هذا المسار **نشط، مو مؤجَّل** — هو الهدف الأساسي فعلياً
> (تاجر سلة). واتساب/الموقع (المسار 1) هما قنوات الوصول لنفس التاجر، مو بديل عنه.
> الترتيب: وصف (تم) ← صور (بيتا أدمن، تم) ← **رفع بالجملة (التالي)** ← اعتماد سلة
> يمشي بالخلفية بالتوازي (لا يعطّل بناء الميزات، بس يعطّل الإطلاق للتجار الحقيقيين).

### م2.1 — إجراءات سلة (تبدأ **اليوم**، بالتوازي) — **أنت**
سلة تأخذ أسابيع للمراجعة؛ كل يوم تأخير هنا = يوم تأخير بإطلاق التجربة للتجار الحقيقيين
(البناء نفسه لا ينتظرها — راجع م2.2).

- [ ] Scopes: `products.read_write, orders.read, customers.read, carts.read, webhooks.read_write, offline_access`
- [ ] وصف التطبيق + الأيقونة ببوابة الشركاء
- [ ] الصفحات المضمّنة (Embedded Pages)
- [ ] اختبار متجر ديمو: تثبيت → `app.store.authorize` يصل → المنتجات تظهر
- [x] Webhook URL + استراتيجية Signature ✅
- [x] صفحة سياسة الخصوصية ✅

> راجع `.claude/skills/salla-integration/SKILL.md` — يوثّق مطبّات حقيقية واجهناها.

### م2.2 — عمق منتج سلة (نشط) — **أنا**
بالترتيب الصارم — **مطابق لأولوية العميل: وصف ← صور ← بالجملة**. لا ميزة جديدة قبل
اكتمال ما قبلها.

| # | المهمة | الحالة |
|---|---|---|
| B1 | تسجيل حساب بالواجهة | ✅ **تم** (`e4a3064`) |
| B2 | **وصل الاستوديو** — `/api/copy` + نشر مباشر على سلة | ✅ **تم** (`af26b56`) — تبويب "وصف المنتجات" بالداشبورد |
| B2.5 | صور المنتجات | ✅ **تم، بيتا أدمن فقط** (`33abd24`) — عمداً غير متاح للتاجر بعد |
| B3 | **رفع منتجات بالجملة** — التالي | ⬜ **لا يوجد كود حالياً** — بند جديد (تحقّقت: صفر ملفات bulk/batch بالمشروع) |
| B4 | ربط سلة ذاتي end-to-end من الداشبورد (بدل التثبيت اليدوي الحالي) | ⬜ |
| B5 | عرض بيانات المتجر الحقيقية بعد الربط (تبويب "متجري" — مبني جزئياً بالداشبورد) | 🟡 موجود، يحتاج اختبار مع متجر حقيقي |
| B6 | تطبيق حدود الباقة المجانية بـ`meter.js` (الأرقام أدناه) | ✅ **تم** (commit تالٍ) — حصة شهرية بدل يومية، ٦٠ وصف + ٣٠٠ رسالة/شهر، سقف ٢٠ متجر تجريبي بالتسجيل |

**B3 (رفع بالجملة) — تصميم أولي مقترح، للمراجعة قبل البناء:**
- رفع CSV أو نسخ/لصق قائمة منتجات (اسم + سعر + فئة لكل سطر)
- تشغيل `copy.js` على كل سطر بالتتابع (تحترم `checkAndConsume` — لا نتجاوز حصة اليوم)
- معاينة قبل النشر الجماعي، ثم نشر دفعة واحدة عبر `salla.js` (`updateProduct` بحلقة)
- **يحتاج قرار:** سقف عدد المنتجات بالدفعة الواحدة (حصة AI + حدود Salla API rate limit)

### م2.2.5 — الموضع التنافسي + حدود الباقة المجانية (قرار 2026-08-07)

**لا ننافس بعرض شامل من اليوم الأول.** نستهدف شريحة محددة: **متاجر سلة الصغيرة
والمتوسطة اللي تبي وصف منتجات + مساعد عملاء سعودي، مجاني وسهل** — لا حملات واتساب
جماعية، لا صندوق وارد متعدد القنوات، لا مكالمات صوتية، لا مئات التكاملات. هذي مزايا
تحتاج بنية تجارية أكبر مما نملك الآن (راجع الفحص المالي أدناه — سعتنا المجانية محدودة
فعلياً، مو تخمين).

**رسالة التموضع (بدل "شات بوت ذكاء اصطناعي" العامة):**
> مساعد مجاني لمتاجر سلة، يكتب وصف منتجاتك باللهجة السعودية ويرد على عملاءك من نفس
> بيانات متجرك.

**الميزة الحقيقية:** دمج دورة كاملة (بيانات المنتج → وصف مقنع → أسئلة عميل → اقتراح
منتج → رابط شراء) بمنتج واحد، لا أداتين منفصلتين كمعظم منافسي سلة الحاليين.

**العميل المستهدف بالضبط:**
متاجر جديدة على سلة يديرها صاحبها بنفسه، ٢٠-٢٠٠ منتج، قطاع إكسسوارات/عطور/ملابس/هدايا،
يكتب الأوصاف يدوياً حالياً، بلا موظف خدمة عملاء، يعتمد على إنستغرام وواتساب بالبيع.
**لا نبدأ بمتاجر كبيرة** — تحتاج تكاملات وحجم محادثات يتجاوز حدود الإطلاق المجاني.

**حدود الباقة المجانية — مُتحقَّقة رياضياً مقابل السعة الحقيقية (راجع الفحص المالي
2026-08-07 بالمحادثة، ومقارنتها بـ`docs/COMPETITOR_GABSTER.md`):**

| الحد | القيمة | التحقق |
|---|---|---|
| متاجر الإطلاق التجريبي | ١٥-٢٠ | يطابق حسابي المستقل: ~١٧-١٨ متجر/يوم بسعة مجانية (Cloudflare+Groq+OpenRouter) عند استخدام خفيف |
| وصف منتجات | ٦٠/شهر لكل متجر | = ٢/يوم — ضمن السعة (٤٠-١١٦ وصف/يوم لكل المشروع) |
| رسائل AI | ٣٠٠/شهر لكل متجر | = ١٠/يوم — ٦٠/شهر + ٣٠٠/شهر يطابقان **بالضبط** سيناريو "الاستخدام الخفيف" (١٠ استدعاءات/يوم/متجر) من الفحص المالي |
| جلسات محادثة | ١٠٠/شهر لكل متجر | — |
| متجر سلة واحد، مستخدم واحد | — | يمنع تعدد الحسابات لتجاوز الحصة |
| القنوات المتاحة | ودجت المتجر + زر تحويل واتساب | **بدون** حملات جماعية، بدون سلات متروكة تلقائية، بدون تعديل طلبات آلي |

✅ **B6 منفّذ:** `meter.js` صار فيه `checkAndConsumeMonthly` بدلتين منفصلتين
(`description` ٦٠/شهر، `message` ٣٠٠/شهر) عبر KV fast-path + D1 مصدر الحقيقة —
`copy.js`، `chat.js`، وويبهوك واتساب (كانت بلا أي حصة إطلاقاً) الثلاثة يستهلكون
منها الآن. `signup.js` صار فيه سقف ٢٠ حساب تجريبي (باستثناء `ADMIN_EMAILS`).
هجرة `0011_monthly_quota.sql` مطبّقة على D1 البعيد، ومُتحقَّق حياً عبر `/api/usage`
(الشكل الجديد بدلتين). الحد اليومي القديم (`checkAndConsume`/`DAILY_LIMIT`) باقٍ
فقط لـ`image.js` (بيتا أدمن، عمداً منفصل).

**ما لا نبنيه الآن (صراحة، عشان الوضوح):** حملات واتساب ضخمة، صندوق وارد متعدد
القنوات، استرجاع سلات متروكة متقدم، مكالمات صوتية AI، تحليلات مبيعات متقدمة، مئات
التكاملات، دعم بشري ٢٤/٧. هذي كلها تستهلك موارد تتجاوز سعتنا المجانية الفعلية —
ونفس النقطة اللي جابستر (منافس أكبر بكثير) يتفوق فيها اليوم، بس مو معركتنا الحالية.

**مؤشر نجاح ٩٠ يوم (بدل عدّاد تسجيلات فارغ):** ١٥-٢٠ متجر نشط، مو مئات التسجيلات
غير المستخدمة. لو ٣٠٪ منهم يستخدمون المنتج أسبوعياً بدقة إجابات عالية — هذا نجاح
حقيقي أقوى من ٥٠٠ تسجيل خامل.

### م2.3 — استلهام تصميمي من المنافسين (أسبوع 3‑4، بالتوازي مع م2.2) — **أنا**
راجع `docs/COMPETITOR_GABSTER.md` كاملاً. **استلهام بصري فقط من الواجهة التسويقية
العامة — لا نسخ حرفي، ولا لمس لأي واجهة داخلية محجوبة خلف تسجيل دخول.**

| # | المهمة | المصدر |
|---|---|---|
| D1 | JSON-LD (`Organization` + `SoftwareApplication`) بصفحة الهبوط | تحسين SEO رخيص، صفر تبعيات |
| D2 | حالات تحميل Skeleton بدل الفراغ — `dashboard.html` (`loadStore`, `loadUsage`) | يقلل شعور البطء وقت جلب بيانات سلة |
| D3 | لقطة/GIF حقيقي للداشبورد الفعلي بدل نص فقط بصفحة الهبوط | بديل أرخص من أداة Arcade المدفوعة |
| D4 | مراجعة تخطيط الأقسام (هيرو، "كيف تشتغل" بثلاث خطوات) لقسم مشابه بصفحتنا | استلهام تنظيم بصري، مو محتوى |

**ممنوع:** أي تبعية خارجية جديدة (Supabase/PostHog/Arcade) — تكسر مبدأ "صفر مفاتيح
API خارجية" الموثّق بـ`README.md`. الأدوات عندهم، الفكرة عندنا فقط.

### م2.3.5 — بنية واجهة مقترحة (تحليل منافسي سلة، قرار 2026-08-07)
تحليل كامل بـ`docs/UX_COMPETITIVE_ANALYSIS.md` — استراتيجية الواجهة: **ليست تقليد
منافسي سلة (مُجيب، كرم، ماندو، جافنا، Trigz، أدوات المتجر AI)، بل مسار واحد أبسط
يجمع وصف المنتج + مساعد خدمة العملاء + التحويل للبيع.** المنافسون يعانون من إعداد
طويل وتشتت بين الوصف/المحادثات/الحملات وغموض حول جاهزية المساعد واستهلاك الرصيد.

| # | البند | يخدم |
|---|---|---|
| E1 | لوحة رئيسية تجيب 4 أسئلة فقط (مربوط؟ متزامن؟ جاهز؟ كم متبقي؟) بدل أرقام مبعثرة | B5، `dashboard.html` |
| E2 | Onboarding خطوة‑بخطوة (ربط سلة → سحب تلقائي للمنتجات/الشعار → سؤال عن الناقص فقط) | B4 |
| E3 | شاشة وصف مقسومة (يمين بيانات / يسار نتيجة) بدل نموذج طويل | B2 (تحسين موجود) |
| E4 | كل رد مساعد يعرض "مصدر الإجابة" + رد صريح عند غياب المعلومة (يطابق قاعدة الصدق بـ`persona.js`) | Studio/Chat widget |
| E5 | حالات فشل/فراغ صريحة لكل تدفق (حصة منتهية، فشل مزامنة، فشل مزود AI...) — رسائل عربية، صفر أكواد خام (`429`) للتاجر | كل الـAPI errors |
| E6 | خريطة صفحات موحدة (`/onboarding /products/:id/description /assistant /conversations /knowledge`) | إعادة تنظيم مستقبلية، ليست هدم للموجود |

**العلاقة بخطة إعادة التصميم الحالية (`quizzical-squishing-iverson.md`):** الهوية
اللونية المعتمدة (`#58A8B4`/`#438FB3`/`#B3B7C1`) لا تتغير — هذا التحليل UX/IA فوقها،
يُنفَّذ تدريجياً ضمن B4/B5/B3 لا كـPR منفصل عاجل. لا عمل كود فوري بدون توجيه صريح.

### م2.4 — الفوترة (أسابيع 4‑6) — **قرار منك + تنفيذ مني**
**لا منتج مدفوع بلا طريقة دفع.** قرار مطلوب: مزوّد الدفع (Moyasar / Tap / Stripe)،
هيكل الباقات، وسياسة التجربة المجانية.

### م2.5 — 🚀 إطلاق تجريبي (أسبوع 6‑8، مشروط باعتماد سلة)
**٥ تجار مختارين يدوياً، مجاناً، مقابل تغذية راجعة صريحة.**

**معايير الخروج:**
- ٣ تجار ربطوا متاجرهم فعلاً بلا مساعدة
- ١٠ أوصاف منتجات مولّدة ومنشورة
- تاجر واحد على الأقل قال صراحة إنه سيدفع

---

## 🟨 مسار مستمر — تقليل الدَين (بالتوازي، بلا تعطيل)

| # | المهمة | الأثر |
|---|---|---|
| ~~C1~~ | ~~حذف/أرشفة endpoints بلا استخدام~~ | ✅ **تم** — من ٥٦ لـ٣٥ endpoint، أُرشفت لـ`docs/archive/deprioritized-features/` |
| C2 | اختبارات منطق حقيقية | الحالية تتحقق من التصدير فقط؛ عزل المتاجر والتواقيع بلا تغطية |
| C3 | معايرة سقوف التكلفة | الأرقام مخمّنة، تُضبط على استخدام حقيقي بعد الإطلاق |
| C4 | استشارة PDPL قانونية | الكود يقدّم تقييماً تقنياً فقط — موثّق بصراحة |
| C5 | تنظيف جداول D1 القديمة | بعد أخذ نسخة |
| C6 | تدريب الوكيل من الأسئلة الشائعة (`docs/UPGRADE_PLAN.md` §6) | باك-إند جاهز (`admin/faq.js`, `storeVectorMemory`)، ناقص واجهة فقط — يوم عمل |
| C7 | Embedded Signup ذاتي لواتساب (`docs/COMPETITOR_GABSTER.md` §3.1) | **مشروط** بطلب حقيقي من تاجر غير أورا — لا يُبنى مسبقاً |

---

## الجدول الزمني المدمج

```
اليوم   1        2        3         أسبوع 2-4        أسبوع 6-8
        │        │        │              │                │
مسار 1  A1,A2 ─► A3-A7 ─► 🚀 إطلاق ─────► تشغيل ومراقبة ──►
(أنت)   (أنت)   (أنا)     محدود          (يخدم عملاء أي تاجر سلة لاحقاً)

مسار 2  م2.1 ────────────────────────► B3 بالجملة ──► B4-B6 ──► 🚀 تجريبي
        سلة (أنت، بالخلفية)             (أنا)                   (مشروط بسلة)

مسار C  ────────────── مستمر بالخلفية ──────────────────────►
```

---

## توزيع المسؤوليات

| أنت (لا أستطيع تنفيذها) | أنا |
|---|---|
| توكن Meta الدائم | كل الكود والاختبار والنشر |
| ربط رقم واتساب الحقيقي | وصل الواجهات بالـ endpoints (ودجت الموقع، consultation، رفع بالجملة) |
| ملف سلة بالشركاء + المراجعة | المراقبة والتنبيه |
| قرار مزوّد الدفع والباقات | تقليل الدَين التقني |
| قرارات المحتوى | التوثيق |

---

## مؤشرات المتابعة (أسبوعياً)

| المؤشر | اليوم | هدف أسبوع 4 | هدف أسبوع 8 |
|---|---|---|---|
| محادثات واتساب حقيقية | 0 | 20+ | 100+ |
| محادثات ودجت الموقع | 0 | 10+ | 50+ |
| حجوزات استشارة | 0 | 5+ | 20+ |
| متاجر سلة مربوطة | 0 | — | ٣+ (سقف تجريبي ١٥-٢٠) |
| أوصاف منتجات مولّدة ومنشورة | 0 | — | 10+ |
| حسابات تجار حقيقية | 1 | 5+ | 15+ |
| زمن كشف العطل | ١١ يوماً | < ١٥ دقيقة | < ٥ دقائق |

**مؤشر ٩٠ يوماً (يحل محل عدّاد التسجيلات):** ١٥-٢٠ متجر نشط، ٣٠٪ منهم يستخدمون
المنتج أسبوعياً بدقة إجابات عالية — أقوى من ٥٠٠ تسجيل خامل.

---

## مبادئ لا تُكسر أثناء التنفيذ

1. **لا إطلاق موسّع قبل تحقق معايير الخروج** — الإطلاق المحدود يكشف الأعطال بأمان.
2. **لا ميزة جديدة قبل وصل الميزات القائمة** — نمط تكرر أكثر من مرة (الاستوديو،
   consultation، والآن ودجت الموقع) يكفي كدرس.
3. **الصدق شرط إطلاق** — لا رقم ولا ادعاء غير حقيقي، ولو بدا العدّاد فارغاً.
4. **كل نشر يمرّ بـ `npm run deploy`** — الحارس يمنع إسقاط الـ API.
5. **لا تُضعف الأمن لتسريع الإطلاق** — الأساس الأمني أثمن ما بُني.
