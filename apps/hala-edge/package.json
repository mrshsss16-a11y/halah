{
  "name": "@hala/hala-edge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hala/config": "workspace:*",
    "@hala/contracts": "workspace:*",
    "hono": "^4.8.9",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.71",
    "@cloudflare/workers-types": "4.20250906.0",
    "@hala/test-kit": "workspace:*",
    "msw": "^2.10.0",
    "vitest": "^3.2.4",
    "wrangler": "4.35.0"
  }
}
