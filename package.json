{
  "name": "hala-ai-os",
  "version": "1.0.0",
  "private": true,
  "description": "Hala AI OS — Saudi AI customer-service agent for Aura Marketing, on Cloudflare Pages Functions + Workers AI + Vectorize",
  "type": "module",
  "scripts": {
    "stage": "node scripts/stage.mjs",
    "build": "node scripts/stage.mjs",
    "dev": "node scripts/stage.mjs && wrangler pages dev dist",
    "test": "node tests/api.test.mjs && node scripts/audit-isolation.mjs",
    "verify:dist": "node scripts/verify-dist.mjs",
    "backup": "node scripts/backup-db.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "deploy": "npm run build && npm run verify:dist && wrangler pages deploy dist --project-name hala-ai-os --branch=main && npm run smoke"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
