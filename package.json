{
  "name": "hala-ai-os",
  "version": "1.0.0",
  "private": true,
  "description": "Hala AI OS — Saudi e-commerce AI marketer on Cloudflare Pages Functions + Workers AI + Vectorize",
  "type": "module",
  "scripts": {
    "stage": "node scripts/stage.mjs",
    "build": "node scripts/stage.mjs && astro build",
    "dev": "node scripts/stage.mjs && wrangler pages dev dist",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name hala-ai-os --branch=main"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
