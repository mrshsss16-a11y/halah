-- خيارات المنتج من سلة (2026-09-10).
--
-- بلاغ صاحب المشروع: الوصف يذكر **اللون الذي رآه النموذج بالصورة وحده**.
-- منتج متوفر بخمسة ألوان يُوصف بلون صورته فقط — نقص بيع، وتضليل للعميل الذي
-- يقرأ "أخضر" ثم يجد خيارات أخرى (أو العكس: يبحث عن الأسود فلا يجده بالوصف).
--
-- سلة ترجّع الخيارات بـ`options[].values[].name`. تُخزَّن JSON مضغوطاً
-- (`[{"name":"اللون","values":["أخضر","أسود"]}]`) لأنها تُقرأ ككتلة واحدة
-- للبرومبت ولا يُستعلم عن قيمة منها منفردة.
--
-- **بيانات تاجر مؤكَّدة** — أعلى ثقة من قراءة الصورة عند التعارض.
ALTER TABLE store_products ADD COLUMN variants TEXT;
