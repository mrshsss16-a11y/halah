# حدود استرجاع مشروع هالة من Cloudflare

## ما تم استرداده

تم حفظ الصفحات العامة المتاحة عبر المتصفح، بما فيها بنية HTML والنصوص المرئية والتنسيقات المضمنة وروابط الأصول العامة. هذه البيانات تكفي لتوثيق واجهة الهبوط وصفحة الدخول كبداية مرجعية لإعادة البناء.

## ما لا يمكن استرداده من النشر العام

| المكوّن | سبب عدم الاسترداد | طريقة الاستكمال الصحيحة |
|---|---|---|
| مصدر المشروع الأصلي | مشروع Pages يستخدم رفعاً مباشراً ولا يملك مصدر Git ظاهرًا | استرجاع المجلد المحلي أو بيئة التطوير الأصلية، أو إعادة بناء مشروع مصدر جديد. |
| دوال Cloudflare Pages | غير متاحة من HTML أو واجهة النشر العامة | إعادة تنفيذها من مواصفات التدفقات، ثم اختبارها في بيئة تطوير. |
| أسرار البيئة | محمية ولا يجب استخراجها أو تخزينها في Git | إعادة إدخالها كأسرار في Cloudflare Pages فقط. |
| D1 وKV وVectorize | بيانات تشغيلية لا تظهر في الواجهة العامة | توثيق المخطط والبيانات اللازمة، ثم ربط المشروع الجديد بالموارد بعد مراجعة الصلاحيات. |
| مصادقة Google وسلة وواتساب | منطق خادمي ومفاتيح OAuth أو Webhook | إعادة إعداد التطبيقات والمفاتيح ومسارات العودة والاختبارات. |
| محادثات العملاء ومحتوى المتاجر | قد تكون بيانات شخصية أو تشغيلية | لا تُنسخ من النشر. تُسترجع فقط بمسار مصرح ومراجع الخصوصية. |

## إجراءات الحماية داخل اللقطة

تم تعطيل روابط الربط وطلبات API داخل صفحات اللقطة. الغرض من ذلك هو منع الصفحة المستردة من إرسال أي طلب إلى إنتاج هالة أو محاكاة مصادقة حقيقية. تظهر روابط ووظائف الإنتاج السابقة بوصفها إشارات توثيقية فقط.

## خطة إعادة البناء

ابدأ بإنشاء مشروع Next.js جديد مع `App Router` وTypeScript وواجهة RTL. بعد ذلك، انقل الصفحة العامة وصفحة الدخول من اللقطة إلى مكونات قابلة للصيانة. أنشئ بعد ذلك طبقات مستقلة للمصادقة، وربط سلة، وإدارة الويبهوك، وD1، وKV، وVectorize، مع اختبارات وحدات وتكامل وE2E. لا تربط بيئة الإنتاج قبل التحقق من كل تدفق في بيئة معاينة مستقلة.
